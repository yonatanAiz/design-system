import {
  ByRoleOptions,
  fireEvent,
  screen,
  within,
} from '@testing-library/react';

export const {{ camelCase name }}Driver = {
  get{{ pascalCase name }}: () => throw new Error('Not implemented'),
};
